apiVersion: apps/v1
kind: Deployment
metadata:
  name: php-apache-custom
spec:
  replicas: 1
  selector:
    matchLabels:
      app: php-apache-custom
  template:
    metadata:
      labels:
        app: php-apache-custom
    spec:
      containers:
        - name: php-apache
          image: registry.k8s.io/hpa-example
          ports:
          - containerPort: 80
          resources:
            requests:
              cpu: 100m
            limits:
              cpu: 500m
---
apiVersion: v1
kind: Service
metadata:
  name: php-apache-custom
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: php-apache-custom
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: php-apache-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: php-apache-custom
  minReplicas: 1
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 50