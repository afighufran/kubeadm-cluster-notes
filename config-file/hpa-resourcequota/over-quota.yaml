apiVersion: v1
kind: Pod
metadata:
  name: over-quota
  namespace: quota-lab
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      requests:
        cpu: "900m"
        memory: 128Mi
      limits:
        cpu: "1"
        memory: 256Mi